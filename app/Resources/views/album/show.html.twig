{% extends 'base.html.twig' %}

{% block body %}
    <h1>Album</h1>

    <table>
        <tbody>
            <tr>
                <th>Titre de l'album</th>
                <td class="espacement"> {{ album.titreAlbum }}</td>
            </tr>
            <tr>
                <th>Artiste</th>
                <td class="espacement">{{ album.artiste }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td class="espacement">{{ album.genre }}</td>
            </tr>
            <tr>
                <th>Support</th>
                <td class="espacement">{{ album.support }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('album_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('album_edit', { 'id': album.id }) }}">Edit</a>
        </li>

        <li>
            <a href="{{ path('commentaire_new') }}">Rédiger un commentaire</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
   {# path('MediaBundle:Commentaire:commentaire_index') #}
{% endblock %}
